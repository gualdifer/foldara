# Deployment Assets

This directory contains platform packaging definitions, service installation assets, signing configuration templates, and release procedures.

Do not commit signing keys, credentials, OAuth secrets, or machine-specific configuration.
